apiVersion: config.openshift.io/v1
kind: OAuth
metadata:
  name: cluster
spec:
  identityProviders:
    - name: {{ .Values.oauth.providerName }}
      mappingMethod: {{ .Values.oauth.mappingMethod }}
      type: HTPasswd
      htpasswd:
        fileData:
          name: htpasswd-secret

