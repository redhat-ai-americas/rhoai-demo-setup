apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    openshift.io/display-name: wb-cpu-storage
  labels:
    opendatahub.io/dashboard: "true"
  name: wb-cpu-storage
  namespace: demo-project
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi